# Backend API URL
# For local development, this will default to http://localhost:3000
# For production, it will use window.location.origin (same domain as frontend)
# Uncomment and set if your backend is on a different domain:
# VITE_BACKEND_URL=https://your-backend-domain.com
